<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dailytask.mapper.TaskTemplateMapper">
    <resultMap id="TaskTemplateResultMap" type="com.dailytask.entity.TaskTemplate">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="TaskTemplateResultMap">
        SELECT id, name, description, created_at, updated_at
        FROM task_template
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="TaskTemplateResultMap">
        SELECT id, name, description, created_at, updated_at
        FROM task_template
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task_template (name, description, created_at, updated_at)
        VALUES (#{name}, #{description}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE task_template
        SET name = #{name},
            description = #{description},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM task_template WHERE id = #{id}
    </delete>
</mapper>
